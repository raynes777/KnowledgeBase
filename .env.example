# JWT Secret (minimo 64 caratteri)
JWT_SECRET=change-this-to-a-secure-random-string-minimum-64-characters-required

# Database Configuration (default in docker-compose.yml)
POSTGRES_DB=ctd_db
POSTGRES_USER=ctd_user
POSTGRES_PASSWORD=ctd_password

# IOTA Configuration (per Phase 2/3)
IOTA_ENABLED=false
IOTA_NETWORK=testnet
IOTA_NODE_URL=https://api.testnet.shimmer.network
